.about-page {
  padding-left: 32px;
  padding-right: 32px;
}

.bio-intro {
  margin-bottom: 40px;
  display: flex;
  flex-direction: column;
  gap: 2px;
}
 
.bio-intro p {
  font-family: var(--font-sans);
  font-size: var(--text-body);
  font-weight: 300;
  color: var(--color-text);
}